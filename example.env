# ============================================================
# dsh-fschannel — 环境变量示例（复制为 .env 后按需修改）
# ============================================================
#
# 凭据（appId/appSecret）不再放在 .env：请在 dsh web 设置页
# 「飞书机器人 -> 连接凭据」中填写，保存到 DSH 凭据库
# （$DSH_HOME/.credentials.yaml，0600 权限，永不回显）。
# 凭据解析分层：插件配置 appId/appSecret > 凭据服务
# （shell 环境变量 > 凭据库 > 项目 .env > ~/.dsh/.env）> 本文件。

# ── 路径配置（本机相关；改为你机器上的实际路径）────────────────────
# 插件仓库根目录（restart-web.ps1 / restart-dsh.ps1 等脚本据此定位；
# 启动 dsh web 时会导出为环境变量，供 cordis.patch.yml 的 !!js 表达式使用）
FSCHANNEL_REPO=/path/to/dsh-fschannel
# .env 文件自身路径（默认取 dsh web 工作目录下的 .env，一般无需设置）
FSCHANNEL_ENV_FILE=/path/to/dsh-fschannel/.env
# 绑定数据文件路径（默认 $DSH_HOME/feishu-bindings.json，一般无需设置）
# 注意：这一项由插件从**本文件**读取，不是从 shell 环境变量读取——
# 在 shell 里 export 它不会生效（其余 FSCHANNEL_* 两者皆可）。
# FSCHANNEL_BINDINGS_FILE=/path/to/feishu-bindings.json
# harness 主目录（默认 ~/.dsh）。凭据库、绑定文件、会话日志都在它下面；
# 插件从进程环境变量读取，通常由 dsh 自己设置，这里列出以备排查。
# DSH_HOME=/path/to/.dsh

# ── 可选：DeepSeek 官方 API Key（dsh 凭据服务的项目 .env 回退源）──
# 未在 $DSH_HOME/.credentials.yaml 或环境变量配置时，新会话的模型
# 请求会从这里读取（与飞书插件无关，dsh 核心功能）。
# DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
