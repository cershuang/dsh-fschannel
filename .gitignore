node_modules/
lib/client.js
lib/client.js.map
# Bundle copies the smoke tests require(); removed on exit, but not if killed.
lib/*.smoke.cjs
lib/*-smoke.cjs
*.log
# Rotation target of restart-dsh.ps1; *.log does not match a .1 suffix.
*.log.[0-9]
.env

# Vision toolkit local artifacts
.dsh-vision-toolkit/

# Feishu staged images (session workspace)
.dsh-fschannel-images/
